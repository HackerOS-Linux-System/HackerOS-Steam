// crystal
// golang
// valac
// meson
// cmake
// libgtk-4-dev

> cd source-code
>>> cd cli && crystal build src/main.cr --release && sudo mv main HackerOS-Steam
>>> cd gui && meson setup build && meson compile -C build
>>> cd tui && go get tui && go build
